/**
 * Converts a string to camelCase format.
 * 
 * This function takes a string with words separated by spaces, hyphens, or underscores
 * and converts it to camelCase where the first word is lowercase and subsequent words
 * have their first letter capitalized with no separators.
 * 
 * @param {string} str - The input string to convert
 * @returns {string} The camelCase version of the input string
 * 
 * @example
 * // Example 1: Convert hyphen-separated string
 * toCamelCase('hello-world-example');
 * // Returns: 'helloWorldExample'
 * 
 * @example
 * // Example 2: Convert underscore-separated string
 * toCamelCase('my_variable_name');
 * // Returns: 'myVariableName'
 * 
 * @example
 * // Example 3: Convert space-separated string
 * toCamelCase('convert this string');
 * // Returns: 'convertThisString'
 */
function toCamelCase(str) {
    // Implementation goes here
}